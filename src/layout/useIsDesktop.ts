/**
 * layout/useIsDesktop.ts — Detecta si corre en una plataforma de escritorio.
 *
 * Portado de pos-mobil. En pos-desktop siempre es true.
 */

export function useIsDesktop(): boolean {
  return true;
}